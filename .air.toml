# Air live-reload configuration for this Go project.
# Run with: air

[build]
  cmd = "go build -o ./tmp/expo ./cmd/expo"
  entrypoint = "./tmp/expo"
  include_ext = ["go"]
  exclude_dir = ["tmp", ".git"]
  delay = 1000
